import React, { FunctionComponent, ReactElement } from "react";

interface Props {}

const Result: FunctionComponent = (props: Props): ReactElement => {
  return <div>Search Result</div>;
};

export default Result;
